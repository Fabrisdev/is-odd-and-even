/**
 * @param {number} i The number to check if it's odd and even
 * @returns {boolean} True if the number is odd and even, false otherwise
 */
export default function isOddAndEven(i: number | string): boolean